# Gradle
.gradle/
build/
**/build/

# Android Studio / IntelliJ
.idea/
*.iml
captures/
.cxx/

# Local config (NO commitear: rutas del SDK del equipo)
local.properties

# Sistema
.DS_Store
Thumbs.db

# Xcode (archivos de usuario/estado local — NO commitear).
# Se ignora solo el estado de usuario; Package.resolved (versiones SPM) SÍ se commitea.
xcuserdata/
*.xcuserstate
iosApp/iosApp.xcodeproj/project.xcworkspace/xcuserdata/
iosApp/iosApp.xcodeproj/xcuserdata/

# Keystore de firma (NO commitear)
*.jks
*.keystore

# Ignore vscode AI rules
.github\instructions\codacy.instructions.md

# Firma de release (claves reales, NUNCA al repo)
keystore.properties

# Claves y ZIPs de firma (nunca al repo)
*.jks
output*.zip

# Herramientas/artefactos de firma de Play Store (nunca al repo)
pepk.jar
upload_certificate.pem

# Artefactos de build de Kotlin/Native (nunca al repo)
.kotlin/

# CI/CD: secretos materializados en el runner (nunca al repo)
play-store-key.json
*.p12
*.p8
*.mobileprovision
sania-release.jks
fastlane/report.xml
fastlane/README.md
